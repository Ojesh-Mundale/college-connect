# Fix Confirmation Email Send Button

## Tasks
- [ ] Add `register` function to AuthContext using supabase.auth.signUp
- [ ] Add `createAfterConfirm` function to AuthContext to create user in backend after confirmation
- [ ] Add backend endpoint for creating user after confirmation
- [ ] Update Register.jsx to use `register` instead of `sendConfirmation`
